
<CompositeView id ="login" width="{dp(360)}" height="{dp(640)}" layout="{layout.container}" >

    <CompositeView id="MainLayout" width="{dp(320)}" height="{dp(400)}" layout="{layout.column}">

        <!-- <TextView
                id="textView"
                text=""
                fontSize="12sp"
                height="{dp(40)}"
                align="{enum.TextView.Align.Center}"
                verticalAlign="{enum.TextView.VerticalAlign.Middle}"
                color="#ffffff"
                /> -->
        <ImageView
                id="QRCodeIv"
                width="{dp(320)}"
                height="{dp(320)}"
                align="{enum.TextView.Align.Center}"
                />
        <RichTextView
            id ="rt"
            color="#ffffff"
            />

        <TextView
                id="TipsTV"
                text="请扫码登录"
                align="{enum.TextView.Align.Center}"
                fontSize="22sp"
                height="{dp(20)}"
                color="#ffffff"
                />


    </CompositeView>

</CompositeView>
